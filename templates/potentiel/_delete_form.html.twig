<form method="post" action="{{ path('app_potentiel_delete', {'id': potentiel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ potentiel.id) }}">
    <button class="btn">Delete</button>
</form>
